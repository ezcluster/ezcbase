

MYDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

. $MYDIR/init.sh

{% for node in m.cluster.nodes %}
echo "Will start {{ node._os_name }}" && openstack server start {{ node._os_name }} &
{% endfor %}
wait
